# ─────────────────────────────────────────────────────────────────────────────
# Clickbait Detection — Python Dependencies
# Install: pip install -r requirements.txt
# Python  : >= 3.9
# ─────────────────────────────────────────────────────────────────────────────

# Core data science
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3

# Gradient boosting (swap in xgboost for GPU-accelerated training)
# xgboost>=2.0     # uncomment and install separately if available

# NLP (optional upgrade — not required for src/ modules as written)
# nltk>=3.8        # enables WordNetLemmatizer for higher-quality lemmatisation
# spacy>=3.7       # alternative: python -m spacy download en_core_web_sm

# Visualisation
matplotlib>=3.7
seaborn>=0.13

# Jupyter
notebook>=7.0
ipykernel>=6.0